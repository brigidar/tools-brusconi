<tool id="Filter_snp_table_assembly" name="filter_snp_table_assembly" version="0.3">
    <description>Filtered SNP table and fasta from assembies</description>
    <requirements>
        <requirement type="package">biopython</requirement>
        <requirement type="package">pandas</requirement>
    </requirements>
    <command interpreter="python">
        $__tool_directory__/2_sort_2.py
        -s ${snp_table}
        -o output_file.fasta
        -t total.txt
        -r $remove
    </command>

    <inputs>
        <param format="txt" name="snp_table" type="data" label="Unfiltered snp table" help=""/>
        <param name="remove" type="text" label="Write True if you want to remove non-canonical nucleotides" help=""/>

        
    </inputs>

    <outputs>
        <data format="fasta" name="output_file.fasta" label="${snp_table.name}_snp.fasta" from_work_dir="output_file.fasta">
            </data>
        <data format="tabular" name="total" label="filtered_${snp_table.name}.txt" from_work_dir="total.txt" >
            </data>
    </outputs>
    
    <tests>
    <!-- Empty -->
    </tests>

    <help>
.. class:: infomark

**What it does**

Removes ambigous SNPs within the same genome and positions with no hit. Optional to remove SNPs derived from non-canonical nucleotides predicted from assemblies.

-----

.. class:: infomark

**Tool Author**

Brigida Rusconi
    </help>
</tool>
