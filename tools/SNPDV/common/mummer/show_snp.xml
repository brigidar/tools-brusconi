<tool id="show_snp" name="show_snp" version="0.1">

    <description>show_snp</description>

    <command>
       show-snp ${command_extra} ${input_delta}
       >$out_tool
    </command>
    <stdio>
       <regex match="1\: PREPARING DATA*\# CONSTRUCTIO" 
           source="stderr" 
           level="warning"
           description="Just ignore this message that goes to stderr" />
    </stdio>


    <inputs>
        <param type="data" format="tabular" name="input_delta"/>
        <param name="command_extra" type="text" size="40" value="" label="Extra cmd line options" help="see specific cmd line options below" />
        
    </inputs>
    <outputs>
        <data name="out_tool" format="text" label="${input_delta.name}"/>

    </outputs>
    
    <tests>
    <!-- Empty -->
    </tests>
    <help>
**Show-snps**

.. class:: infomark

**What it does**

show-snps is a utility program for reporting polymorphisms contained in a delta encoded alignment file output by NUCmer or PROmer. It catalogs all of the single nucleotide polymorphisms (SNPs) and insertions/deletions within the delta file alignments. Polymorphisms are reported one per line, in a delimited fashion similar to show-coords. Pairing this program with the appropriate MUMmer tools can create an easy to use SNP pipeline for the rapid identification of putative SNPs between any two sequence sets, as demonstrated in the manual SNP detection section.



http://mummer.sourceforge.net/manual/#snps

-----

.. class:: infomark

**Wrapper Author**

Brigida Rusconi
    </help>
</tool>
